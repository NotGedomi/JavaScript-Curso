// Convertir de numero a cadena
let a = 10, b = 20;
// Si son valores numéricos, se suman
console.log(a+b); // Resultado: 30

// Entonces convertimos de numero a cadena
let concatenacion = a.toString() + b.toString(); // toString() (pasar a string)
console.log(concatenacion); // Resultado: '1020'